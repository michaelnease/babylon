const SEP = "/";

const APP = "app";
const CHARACTER = "character";

const BOOT = "init";
const GET_CHARACTERS = "getCharacters";

// Action Types
export const APP_BOOT = APP + SEP + BOOT;
export const CHARACTER_GET_CHARACTERS = CHARACTER + SEP + GET_CHARACTERS;
